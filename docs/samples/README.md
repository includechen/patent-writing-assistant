# docs/samples — 真实生成成果样例

本目录文件来自本机 `%APPDATA%\patent-assistant\data\outputs\` 的实际产出（专利：**一种基于应用启动预热的性能优化方法**），供 README 引用，**非虚构内容**。

| 文件 | 说明 |
|------|------|
| `disclosure-excerpt.md` | 交底书 Markdown 节选（前 ~95 行） |
| `flowchart-example.mmd` | Phase 2b 生成的 Mermaid 流程图源码 |
| `chapters-example.json` | Phase 章节结构化 JSON（若存在） |

更新样例：在本机完成一次新专利撰写后，可复制新的 `.md` / `.mmd` 覆盖上述文件并提交 Git。
